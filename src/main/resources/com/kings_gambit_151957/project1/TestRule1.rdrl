package com.kings_gambit_151957.project1;

import java.lang.Number;

rule "TestRule1"
	ruleflow-group "test1"
	dialect "mvel"
	when
		choice : TestChoice( decision == "yes" )
	then
		TestEquipment equipment = new TestEquipment();
				equipment.setExperience( 50 );
				equipment.setSkillPoint( true );
				equipment.setItems( Hjalmar's sword );
				insert( equipment );
end
